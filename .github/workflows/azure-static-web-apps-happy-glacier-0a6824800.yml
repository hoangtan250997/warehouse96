name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - feat/search
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - feat/search

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
<<<<<<<<< Temporary merge branch 1
      - name: Clear npm auth
        run: |
          NPM_USER_CONFIG=$(npm config get userconfig)
          echo "Clearing: $NPM_USER_CONFIG"
          echo "registry=https://registry.npmjs.org/" > "$NPM_USER_CONFIG"
      - name: Install dependencies
        run: npm install --prefer-offline
        env:
          NODE_AUTH_TOKEN: ''
          NPM_TOKEN: ''
=========
      - name: Install dependencies
        run: |
          echo "registry=https://registry.npmjs.org/" > /tmp/clean.npmrc
          npm install --prefer-offline
        env:
          NPM_CONFIG_USERCONFIG: /tmp/clean.npmrc
>>>>>>>>> Temporary merge branch 2
      - name: Build app
        run: npm run build
      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_HAPPY_GLACIER_0A6824800 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "dist/WarehouseFE/browser"
          api_location: ""
          skip_app_build: true
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_HAPPY_GLACIER_0A6824800 }}
          action: "close"
