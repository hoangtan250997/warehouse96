name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - feat/search
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - feat/search

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: |
          printf 'registry=https://registry.npmjs.org/\n' > /tmp/npm-user.npmrc
          printf 'registry=https://registry.npmjs.org/\n' > /tmp/npm-global.npmrc
          unset NODE_AUTH_TOKEN
          unset NPM_TOKEN
          unset NPM_CONFIG__AUTHTOKEN
          npm install --registry https://registry.npmjs.org/ || (cat /home/runner/.npm/_logs/*.log && exit 1)
        env:
          NPM_CONFIG_USERCONFIG: /tmp/npm-user.npmrc
          NPM_CONFIG_GLOBALCONFIG: /tmp/npm-global.npmrc
      - name: Build app
        run: npm run build
      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_HAPPY_GLACIER_0A6824800 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "dist/WarehouseFE/browser"
          api_location: ""
          skip_app_build: true
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_HAPPY_GLACIER_0A6824800 }}
          action: "close"
